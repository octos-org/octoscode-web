{
  "name": "@octos-org/octoscode-web-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @octos-org/octoscode-web dev",
    "mock:server": "pnpm --filter @octos-org/octoscode-web mock:server",
    "build": "pnpm --filter @octos-org/octoscode-web build",
    "contract:verify": "node scripts/sync-core-contract.mjs --check",
    "contract:update": "node scripts/sync-core-contract.mjs --write",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "oxlint --deny-warnings apps packages e2e playwright.config.ts",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1",
    "oxlint": "1.76.0",
    "prettier": "^3.9.0",
    "typescript": "~5.9.3",
    "vitest": "^4.1.8"
  }
}
